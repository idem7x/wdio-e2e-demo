# CI overlay — applied on top of docker-compose.yml on Linux runners
# where /dev/kvm is available. Enables hardware-accelerated emulator.
#   docker compose -f docker-compose.yml -f docker-compose.ci.yml up -d
services:
  android:
    devices:
      - /dev/kvm
